# betterStealthPath 功能规格（FEATURE.md）

> 本文档是"洁净室再实现"用的**设计规格**：只描述 Stealth Path / 偷袭小道（Mindustry 客户端路线辅助模组，原作 DeterMination，当前发布版本 6.0.4）要解决什么问题、为谁解决、用户可见哪些功能、行为边界在哪。不包含任何实现细节。后续工程师应只依据本文档 + 公开的 Mindustry/MindustryX 资料，重新实现一个更好的同类模组。

## 1. 产品定位与设计哲学

### 1.1 解决什么问题

Mindustry 进攻作战中，玩家（尤其是 RTS 式框选大量单位的玩家）很难在纷繁的敌方炮塔射程、单位火力、深水与矿渣等地面危险之间，快速判断"哪条路打过去损失最小"。原版游戏不提供任何威胁可视化或风险预估。Stealth Path 把"敌方火力场"翻译成一条**看得懂的路线参考**：按住一个键，地图上画出一条从你的单位到目标的、预估受伤更低（或可选最近）的路线，并标注预计受伤数值；再进一步，可以让选中的单位沿这条路线自动行进。

### 1.2 目标用户

- 需要指挥大量单位集群进攻、经常面对复杂敌方防线的玩家（PVE 推图、PVP 突袭）。
- 希望在行动前先估计风险、减少"一队兵走进炮塔阵全灭"的玩家。
- 使用 MindustryX 客户端的玩家可获得更好的悬浮窗体验，但普通客户端用户同样是第一等用户（有完整回退）。

### 1.3 核心设计原则

1. **本地决策参考优先**：模组产出的是"画在你屏幕上的路线 + 伤害估计"，是否采纳由玩家决定。
2. **公平性边界（红线）**：
   - 不改变任何单位属性、伤害数值、服务器规则；
   - 对游戏局的唯一外部影响 = 用 Mindustry **原生的单位指挥通道**给自己选中的单位下发移动路点，等价于玩家手动右键指挥，服务器无需安装任何东西；
   - 不自动开火、不下发攻击指令，只做移动；
   - 模组标记为 hidden（不在模组列表显眼展示），避免多人服模组校验冲突与不必要的注意。
3. **零依赖、可回退**：不引入第三方库；对 MindustryX 的所有增强（悬浮窗等）在缺失时静默回退到原版 HUD，核心路线功能不受影响。
4. **低干扰、显式触发**：不按键就什么都不发生——没有持续的自动行为、不抢占游戏输入；提供可选修饰键防止误触。
5. **"实验性、默认克制"的口径**：README 向玩家宣称功能仍在完善、需到设置中手动开启、自行绑定按键。注意：原实现的总开关实际默认为开、按键也预置了默认键位（X/Y/N/M 等），与宣传口径存在偏差——"更好版本"应统一口径（建议：总开关默认开，但首次使用前给一次性的引导提示；或真正默认关）。
6. **联机友好（不做服务器的负担）**：自动移动的指令下发有节流设计（路点数上限、重发间隔、指令间隔、分批），避免被服务器反 DoS 机制误判踢出。

### 1.4 威胁模型：防什么、不防什么

**防（纳入威胁评估的）**：
- 敌方炮塔：射程、最小射程（内圈盲区）、冷却剂加成/效率修正后的估算 DPS；普通弹、激光/持续束、牵引束等类型分别对待。
- 敌方单位（可选包含）：按单位火力估算威胁。
- 地面危险：矿渣等地块伤害按"实际通过时间"累计；水/冷冻液等地块减速计入通过速度；深水对可溺水地面单位是沉没风险。
- 力场护盾（force projector）覆盖区：视为不可通行（单位过不去会卡住）。
- 集群队形：路径按整队的通行宽度/威胁净空规划，不只是单点。

**不防（明确不保证的）**：
- 估算 ≠ 精确仿真：伤害是统计口径的估计值（原版把每个炮塔近似为"环形 DPS 场"），不预测玩家操作、敌方微操、弹药类型切换等动态。
- 不防"人为埋伏"：只看当前可见的世界状态（已建造的炮塔、现存单位）。
- 路径"安全"只意味着**预估**受伤低/为零，不构成胜负保证。

### 1.5 已知设计弱点 / 社区批评 / 改进机会（调查所得）

- **威胁近似过粗**：把每个炮塔当成均匀 DPS 圆，忽略弹种形状（激光是线、溅射是面、碎片弹会散开）、命中概率、护甲减伤、状态效果（点燃/减速的持续伤害与代价）、多目标上限（一门炮同时只能打 N 个目标，大队形实际受伤远低于 DPS×时间）。作者自己的实验分支（threat-model 方向，见 9.1）已在验证"按编队与弹体形状采样威胁"的修正方案。
- **威胁过滤手动化**：原版"陆/空/混合"过滤在首次使用时按玩家当时单位自动猜一次，之后就固定为手动；选了混合编队时不自适应。实验分支已提出"Auto 档"（每次规划按当前选中单位的构成自动推断）。
- **深水/液体边缘的脆弱性**：v5.2.2 之前存在"跨矿渣+深水时路径规划直接失败"的死角，靠"目标候选半径"（在目标周围一圈找替代终点）补救——说明液体边界场景需要被当作一等设计约束。
- **零自动化测试**：原仓库没有任何测试任务，验证全靠手测清单。回归只能靠人肉，这是质量的根本短板。
- **设置面板暴露实现细节**：把"寻路算法 A*/DFS"这类纯实现选择暴露给玩家，玩家无从判断；应隐藏或替换成玩家能理解的语义（如"速度优先/谨慎优先"）。
- **文案与多语言维护**：仅英文/简中是全量文案（约 260 行），其余 30 余种语言是 68~114 行的部分翻译；设置/提示更新时容易漏同步。
- **更新器较激进**：内置"下载并替换 mods 目录中当前模组文件并自动重启"（桌面端）。对玩家来说方便，但替换自身文件 + 重启属于侵入式行为，且依赖自建镜像前缀。
- **移动端是"死重"**：安卓包能加载但所有交互功能直接关闭；安卓用户实际拿不到任何功能，只是不崩溃。
- **主入口过于庞大**（维护性）：历史累计导致单文件承载全部行为，拆分正在进行中——对再实现者的教训是模块边界要一开始就划好。

## 2. 功能清单（用户视角，黑盒行为规格）

### 2.1 手动路线预览（核心功能）

- **触发**：点按或按住"仅敌方炮塔"（默认 X）或"炮塔+单位"（默认 Y）。
- **行为**：以"起点单位"（见 2.4）为起点、按当前目标模式选终点，计算一条低受伤路线并画在地图上：一条彩色线 + 可选的起点/终点圆点 + 可选的"预计受伤"数字（默认显示在路径中段，可改为末端）。同时在屏幕上弹出提示："路径：N 格，预计受伤 X"。
- **按住语义**：按住期间按"预览刷新间隔"（默认 0.10s）持续重算，鼠标/战场变化时路线实时跟随。
- **保留时长**：松开后路线按"路径显示秒数"（默认 10s）保留后消失；设为 0 表示一直保留到下次计算。
- **边界情况**：不在游戏中 / 无玩家单位 / 无敌方目标 / 无可达路径 → 弹出对应提示（"未找到敌方目标"/"未找到路径"），不绘制；起点不可通行时放弃本次计算；输入焦点在对话框时按键不响应。

### 2.2 目标模式（路线终点怎么选）

按"切换显示模式"键（默认 K）循环五档，切换时弹 toast 反馈：
1. **最近的敌方核心**（默认）：同时规划到最近 K 个敌方核心（K 可设，默认 1，最多 12），逐条画出，提示"核心路径：N 条，最小受伤 X"。
2. **最近的敌方建筑**。
3. **最近的指定建筑**：玩家在设置里选一种建筑类型（仅本模式可选）。
4. **敌方发电集群**：把敌方发电建筑聚成群（火力发电机、涡轮冷凝机这类低价值发电机不算），对最有价值的至多 N 群各画一条渗透路线（默认 3 条；群至少 2 座；可选从玩家位置出发或从"靠近敌方炮塔的一侧"出发）。提示"发电集群路径：N 条，最小受伤 X"。
5. **玩家→鼠标位置**：终点跟随鼠标；可在设置中把终点来源换成"可拖动的目标点"（屏幕上显示一个圆圈+十字准星，可拖动，可隐藏）。

### 2.3 威胁过滤（陆/空/混合）

- "威胁过滤"键（默认 L）循环：陆军 / 空军 / 空军+陆军。
- 首次使用（未手动设置过）时，按玩家当前单位是否会飞自动选定一档。
- 语义：陆军单位只看对地火力，空军只看对空火力；过滤错误会导致路线误判，所以这是最重要的快捷开关之一。

### 2.4 自动模式（单位集群持续规划）

- **N（自动：集群→鼠标）**：开启后持续规划"选中单位 → 鼠标位置"的路线；再按一次关闭。
- **M（自动：集群→聊天坐标）**：目标来自聊天里出现的 `(x,y)` 格子坐标（自己或队友发送的报点均可），超界坐标会钳制到地图内；按 M 时若还没有任何坐标缓存，提示"请在聊天发送 (x,y) 设置目标"。
- **起点与集群**：优先用玩家框选的 RTS 单位；没有框选则用玩家当前操控的单位。相距超过"集群分割距离"（默认 5 格）的选中单位拆成多个集群，各自规划、各自绘制、各自指挥，以保持队形。
- **静止降频**：起点、目标、威胁过滤都没变且编队静止时，重算间隔按倍率（默认 8 倍）拉长以省性能——路线"不变不动"是预期行为不是卡死。
- **路径颜色语义（自动模式专属）**：预估最大受伤 < 阈值（默认 10）→ 绿（安全）；≥ 阈值但有单位能活着走完 → 黄（警告）；≥ 集群中同类最脆单位的血量（即预计会死人）→ 红（致命）。三色均可用十六进制自定义。

### 2.5 自动移动（沿路线下发移动指令）

- **触发**：在 M/N 自动模式开启且"自动移动开关"（默认开）打开时，选中单位沿最低受伤路线获得移动指令；"自动移动"热键（默认鼠标右键）用于立即下发/接管沿当前路线的移动。
- **行为语义**：把规划路线转成有限个路点（上限默认 12），通过原生 RTS 指挥通道发给**自己选中的单位**；第一个路点立即下发，其余按节奏排队；单位接近路点（默认 2 格内）即推进下一路点；路线与上次相比没有实质变化时不重复下发指令。
- **联机节流**：重复整路重发有最小间隔（默认 0.5s）；每条指令包之间有额外间隔（默认约 0.03s）；大规模编队（约 ≥12/16/24）分批下发、大单位先行——目的是不触发服务器反 DoS。
- **兜底**：完全找不到安全路线时，退化为"直接移动到目标点"。
- **边界情况**：无选中可控单位 → 提示"未找到可控单位群（请先框选单位）"；单位死亡或世界切换 → 路径与自动状态清空重算；移动端本功能整体关闭。

### 2.6 液体与深水防护（地面单位专用语义）

- 可溺水的地面单位，其路线若穿过"会淹死"的深水段，规划器会反复绕开（最多几轮封锁-重搜）；确实绕不开时才考虑"可生存液体穿越"（默认允许，但前提是整条路径的沉没判定通过）。
- 沉没判定有"预留时间"（默认 1.5s）安全余量；进入液体段前保持严格到点，不提前推进路点，避免单位被卡在岸边。
- 目标被液体围死时，在"目标候选半径"（默认 24 格）内找替代终点重试，避免整条规划直接失败。

### 2.7 威胁可视化（调试向）

- **风险热力图**：默认 J 键切换，把威胁网格数值叠加在地图上（字号可调），用于直观验证"模组眼中的危险区"。
- **悬停炮塔 DPS**：默认关；开启后在悬浮窗中列出鼠标所指格位受各炮塔影响的 DPS 明细与总值（有输出节流）。

### 2.8 OverlayUI 窗口（MindustryX 增强，缺失时降级）

装 MindustryX 时，模组提供可拖拽/可缩放/可管理的悬浮窗口：
1. **模式窗口**：当前路径模式 + 威胁过滤。
2. **伤害窗口**：当前路线预估受伤。
3. **控制窗口**：X/Y/N/M/威胁过滤等快捷按钮；窗口窄时按钮竖排、宽时多列，可拉伸到任意尺寸不回弹。

**降级行为（无 MindustryX）**：同样内容以固定位置（左上角堆叠）的普通 HUD 元素显示，功能等价；三个窗口各有独立开关（同时管 OverlayUI 与 HUD 回退两态）；玩家在 OverlayUI 面板里的显示/钉住选择不会被模组强行覆盖。

### 2.9 更新检查

- 进入主菜单后检查 GitHub Release（6 小时节流一次，可整体关闭）：有新版本时弹窗列出可下载文件、当前/最新版本号，支持"打开 GitHub / 忽略该版本 / 稍后"；桌面端支持"下载并重启"（显示百分比、已下载/总大小、实时速度；完成后替换 mods 目录中的当前模组文件并自动重启）与镜像下载开关（为 GitHub 受阻地区准备的地址改写）。
- 关闭"弹窗提示"后只显示轻量提示不弹窗；整体关闭后完全静默。

## 3. 设置面板规格

入口：`设置 → 模组 → 偷袭小道`。单页分组 + "专业模式"解锁的高级折叠区。所有项**即时生效**（颜色/线条/阈值类立即反映到下一次绘制；没有需要重启的项）。

### 3.1 常规（General）

| 设置（显示名语义） | 默认 | 取值 | 说明 |
| --- | --- | --- | --- |
| 启用（总开关） | 开 | 开/关 | 关闭后一切绘制/热键/自动行为停止 |
| 显示提示 | 开 | 开/关 | 模式切换、无路径等 toast |
| F8 控制台调试日志 | 开 | 开/关 | 规划/自动/下发/溺水四类详细日志 |
| 专业模式 | 关 | 开/关 | 解锁高级折叠区（未解锁时显示解锁按钮） |
| 始终规划最近路径 | 关 | 开/关 | 忽略伤害按最短路规划 |
| 以最慢单位作为代价基准 | 开 | 开/关 | 集群按最慢单位估算时间/受伤 |
| 计入地块状态减速 | 开 | 开/关 | 水/冷冻液等减速进速度估算 |
| 允许可生存液体穿越 | 开 | 开/关 | 见 2.6 |

### 3.2 OverlayUI 与绘制

| 设置 | 默认 | 取值 |
| --- | --- | --- |
| 模式窗口 / 伤害窗口 / 控制窗口 | 各自开 | 开/关 |
| 路径显示秒数 | 10s | 0–60s（0=永久保留） |
| 线条粗细 | 2 | 1–6 |
| 路径透明度 | 85% | 0–100% |
| 显示端点 | 开 | 开/关 |
| 起点圆点大小 | 220% | 0–400%（相对线宽） |
| 终点圆点大小 | 260% | 0–400% |
| 显示伤害文字 | 开 | 开/关 |
| 伤害文字缩放 | 60% | 20–140% |
| 伤害文字放在末端 | 关 | 开/关 |
| 伤害文字偏移 | 100% | 0–300% |
| 预览刷新间隔 | 0.10s | 约 0.02–1s |

### 3.3 颜色（十六进制文本输入，支持 6/8 位含透明度）

发电集群路径 `3c7bff`；玩家→鼠标路径 `a27ce5`；自动模式安全 `34c759` / 警告 `ffd60a` / 致命 `ff3b30`。

### 3.4 自动（Auto）

| 设置 | 默认 | 取值 |
| --- | --- | --- |
| 自动安全伤害阈值 | 10 | 0–200 |
| 自动移动开关 | 开 | 开/关 |
| 指令重发间隔 | 0.50s | 约 0.02–4s |
| 指令包间隔 | 约 0.033s | 0–约 0.17s |
| 路点到达半径 | 2 格 | 0–8 格 |
| 自动计算分帧 | 1 tick | 1–60 tick |
| 自动威胁扩展上限 | 6 格 | 0–20 格（找不到安全路径时最多放宽多少） |

### 3.5 发电集群 / 目标 / 更新

- 发电集群：绘制条数 3（1–10）；最小群规模 2（2–10）；从玩家开始（默认关）。
- 目标：威胁过滤三档按钮（陆/空/混合，初始未设=首次自动选定）；目标模式五档按钮；目标建筑选择器（仅"指定建筑"模式可用）；路径目标来源（鼠标/目标点）；显示目标点标记（开）；敌方核心数量 K=1（1–12）。
- 更新：检查更新（开）、弹窗提示（开）；镜像开关在更新弹窗内而非设置页。

### 3.6 高级（需专业模式；分组：寻路 / 自动移动 / 缓存性能 / 发电集群）

寻路：算法选择（A*，或备用 DFS——**建议更好版本移除此类实现选项**）；集群分割距离 5 格（1–30）；队形膨胀系数 125%（100–300）；安全走廊居中偏好 35%（0–200%）；计算安全距离图（开，关掉可提速）；深水预留时间 1.5s（0–3.0s）；目标候选半径 24 格（6–64）；风险调试文字大小 100%（50–400%）；显示鼠标格炮塔 DPS（关）。
自动移动：路点上限 12（2–60）；分批（开）；分批大小倍率 100%（50–200%）；分批延迟倍率 100%（0–200%）；静止降频倍率 8x（1–30x）。
缓存性能：可通行性缓存条目数 8（1–24）。
发电集群：连接距离 8 格（1–20）；靠近炮塔距离 12 格（1–40）；最小绘制长度 12 格（1–60）；回退格数 16（0–60）。

## 4. 按键/操作规格

| 功能 | 默认 | 语义 |
| --- | --- | --- |
| 仅敌方炮塔预览 | X | 点按算一次；按住=实时预览 |
| 炮塔+单位预览 | Y | 同上（威胁包含敌方单位） |
| 修饰键 | **未绑定** | 绑定后所有功能键都需"修饰键+功能键"组合触发，防误触 |
| 切换目标模式 | K | 循环五档，toast 反馈 |
| 切换威胁过滤 | L | 循环陆/空/混合，toast 反馈 |
| 自动：集群→鼠标 | N | 开关式（toast 反馈开/关） |
| 自动：集群→聊天坐标 | M | 开关式；目标来自聊天 `(x,y)` |
| 自动移动 | 鼠标右键 | 在 M/N 模式下沿路线下发移动 |
| 风险热力图 | J | 开关调试叠加层 |

全部可在 `设置 → 控制` 改键。移动端无按键交互。

## 5. 多语言范围

- 交付 35 个语言文件：默认英文 + 34 个区域 locale（含 zh_CN/zh_TW/ja/ko/ru/fr/de/es/pt_BR/pt_PT/it/nl/nl_BE/pl/tr/vi/th/id_ID/fil/eu/ca/cs/da/et/fi/hu/lt/ro/sr/sv/tk/uk_UA/be/bg）。
- **完整维护的只有英文与简中**（设置名+描述、toast、更新弹窗、悬浮窗、日志、按键名全量约 260 行）；其余语言为部分翻译（约 68–114 行，覆盖旧字符串集）。中英文是文案源头，改设置/提示必须双语同步——这是原仓库的硬性约定。
- 字符串大类：设置项名称与描述、toast 提示（模式切换/无目标/无路径/路径统计）、按键绑定名、OverlayUI 窗口文案、更新检查弹窗与下载进度、调试日志模板、分类/分组标题。

## 6. 部署形态

### 6.1 安装包形态

- **单一合并 jar/zip**：同时包含桌面字节码、安卓 `classes.dex` 与模组描述文件，桌面与安卓通用；安卓可安装性以"包内含 classes.dex"为准。一个 Release 有且只有一个 `.jar` 资产（Mindustry 游戏内安装器取第一个 `.jar`，不区分平台）。
- 目标游戏版本：Mindustry v7（最低构建 154）；字节码兼容 Java 8；`hidden: true`；不依赖任何第三方库。

### 6.2 MindustryX 增强（可选层）

- 有 MindustryX：悬浮窗口接入其 OverlayUI 管理（可拖拽/缩放/记忆位置）。
- 无 MindustryX：回退为原版 HUD 元素，功能等价。
- **进行中的方向（原仓库未提交）**：双入口声明（普通入口 + X 增强入口，X 入口仅在 MindustryX 加载器存在时优先生效，直接装配桥接层）；桥接能力从"悬浮窗"扩展到"地图标记（在指定格位打文字标记）"与"蓝图分享（发到聊天/剪贴板）"，全部带"未安装则空实现回退"的守卫。更好版本应把这层"可选宿主能力"作为一等设计。

### 6.3 独立安装 vs 被聚合（如 Neon 聚合模组）

- **独立形态**：自有设置分类（`设置 → 模组 → 偷袭小道`）、自有按键、自有更新检查，一切自成一体。
- **聚合形态（设计契约）**：作为子模组并入聚合模组时——设置页被聚合总设置页吸收（不再注册独立设置分类，文案仍可由子模组提供）；X 增强入口由聚合方工厂替换装配；发版走聚合仓库流程；子模组独立身份保持不变。原仓库已定义此契约但**尚未实现接入**；更好版本应在第一天才把"独立/聚合"双形态做成显式能力（例如用构建开关注入聚合标记，聚合态下禁用独立更新检查与独立设置页）。

## 7. 质量与非功能需求

- **性能**：大地图 + 大量敌方单位 + 大编队选中时不得明显拖帧。手段（以行为语言表述）：一次规划的工作量可分摊到多帧；威胁网格按世界尺寸复用、切世界释放不累积；通行性结果缓存且在建筑增减时失效；编队静止且目标未变时按倍率降频；路点数有上限；调试输出有节流。长路线绘制只画转折点。
- **兼容性**：Mindustry v7+（最低 154）；MindustryX 任意版本缺失时不得崩溃（历史上曾因引用 MindustryX 特有能力导致无 X 环境崩溃，属必须防御的回归）；多人服务器无需安装；不与服务器模组校验冲突。
- **可维护性期望**：模块边界清晰（输入与模式 / 威胁评估 / 路线规划 / 绘制 / 指令下发 / 设置与悬浮窗各自独立），新增威胁类型或目标模式不应牵动全局；对宿主（MindustryX）的一切访问必须可回退；用户可见文案全部走多语言文件，禁止硬编码。
- **测试期望**：原仓库**没有任何自动化测试**（编译通过 + 打包成功 + 手测清单即全部验证）。更好版本必须补上：威胁数值估算、路径代价/压缩/坐标换算这类纯逻辑应可脱离游戏运行时自动验证（可重复跑的断言式自测）；依赖运行时的行为（预览观感、指令节奏、悬浮窗挂载）保留结构化手测清单，且清单条目应写成可勾选的行为陈述（例如"含深水地图上，可溺水地面单位不会被规划进危险深水段"）。
- **发布质量**：语义化版本号（大功能.小功能.修复）；Release 资产纪律（唯一 `.jar`、禁传中间产物）。

## 8. 明确的非目标（Non-goals）

1. 不修改单位属性、伤害、射程或任何游戏数值；不改服务器规则。
2. 不做服务器端插件；不要求服务器安装。
3. 不自动作战：只下发移动指令，不下发攻击/技能指令，不代替玩家开火。
4. 不追求"最优解保证"或胜利承诺：路线与受伤都是估计值。
5. 不做移动端交互功能（原版仅保证安卓能加载不崩溃）。
6. 不做路线辅助之外的网络功能（更新检查除外）。
7. 不重构游戏本体 UI；只叠加自有的悬浮窗/HUD 元素。
8. 不支持低于 v7 的 Mindustry。

## 9. 给"更好版本"的设计方向备注

1. **编队感知威胁模型（最高优先）**：威胁值应从"选中单位的实际编队（各自位置/半径/血量/护甲）+ 弹种真实形状（直射弹/线状激光/溅射/碎片/连锁/牵引）+ 命中概率 + 多目标上限 + 状态效果持续伤害"推导，而不是把每个炮塔当作均匀 DPS 圆。作者的 threat-model 实验分支已验证此方向（"每格一个威胁值"的对外契约不变，只换推导方式），并顺带给出第二个结论——
2. **威胁过滤 Auto 档**：过滤档位增加"Auto"：每次规划按当前选中单位的地/空构成自动推断，手动选择仅作为覆盖。消除"首次使用猜一次然后固定"的误判源。
3. **可自动验证性从第一天抓起**：威胁估算与路径代价必须设计为可脱离游戏运行时运行并断言（原版零测试是最大质量债）；液体/深水边界场景要进回归用例（历史上反复出问题）。
4. **设置面板去实现化**：砍掉"寻路算法 A*/DFS"这类暴露内部实现的选项；把"距离 vs 风险"的权衡（原版藏在微小的内部偏置系数里）提升为一个玩家可懂的滑杆（例如"谨慎 ←→ 激进"）。
5. **统一"默认关闭"口径**：README 说默认关、实现默认开——更好版本要么真默认关+首次引导，要么改宣传；不要两者不一致。
6. **多语言要么全量要么收缩**：34 个部分翻译文件是维护负担；建议收缩到"英文+简中全量、其余按翻译完成度发布"，并把"双语同步"做成构建期检查。
7. **更新器收敛**：考虑改用游戏原生模组更新机制或至少把"替换自身文件并重启"做成显式确认步骤；镜像列表应可配置而非硬编码单一前缀。
8. **移动端做出取舍**：要么实现触摸等价交互（长按预览、拖动目标点），要么明确声明桌面专用并裁掉安卓产物，不背"死重"。
9. **聚合形态先设计后实现**：独立/聚合双形态（设置页归属、更新检查让位、入口替换）作为第一天的一等设计，而不是事后补契约成员。
10. **玩家信任与公平性说明**：在 README 里明确"本模组只做本地参考 + 原生移动指令"的边界声明，主动回应"辅助算不算作弊"的社区质疑，并可在悬浮窗提供"本局已启用"的自我声明（诚实披露设计）。

## 附：进行中的方向（原仓库未提交工作，不算既有功能）

- 威胁评估模块化重构（编队/弹种感知威胁采样，见 9.1/9.2）。
- MindustryX 双入口与桥接层（悬浮窗/地图标记/蓝图分享三能力，缺失回退空实现，见 6.2）。
- 开发用模组描述文件携带 X 增强入口 + 临时开发身份（`-dev` / `0.0.0`）。
